package OOP.Bai49_50_51_52_Chuoi;

public class LyThuyet {
    /*
    Có 3 loại chuỗi: - String: bất biến
                     - StringBuffer: khả biến, multi_thread
                     - StringBuilder: khả biến, single-thread
     */
    /*
    Các phương thức chuỗi String
     - Kiểm tra lớp String
        + lenght(): độ dài của chuỗi
        + charAt(): lấy ra 1 ký tự ở vị trí k
        + getChars(vị trí bắt đầu, vị trí kết thúc, mảng dữ liệu, vị trí bắt đầu lưu mảng): sao chép ra 1 đoạn của chuỗi thành mảng char
        + getBytes(): lấy ra giá trị của từng ký tự
    Phương thức so sánh lớp String
     - equals(...): so sánh 2 chuỗi giống nhau(có phân biệt hoa, thường)

    /*
      Phương thức tìm kiếm chuỗi
         + indexOf(): tìm kiếm từ bên trái sang bên phải
         + lastIndexOf(): tìm kiếm từ bên phải sang bên trái
      */
    
}
